MAIN 		SEGMENT
		ASSUME	CS:MAIN, DS:MAIN

		MOV	AX, CS
		MOV	DS, AX
		MOV	CX, 1
		MOV	AX, 0	;홀수 합
		MOV	BX, 0	;짝수 합

LOOP1:	ADD	AX, CX
		INC	CX
		ADD	BX, CX
		INC	CX
		CMP	CX, 20
		JBE	LOOP1

		MOV	ODD_SUM, AX
		MOV	EVEN_SUM, BX
		MOV	AH, 4CH 
		INT	21H


ODD_SUM		DW	?
EVEN_SUM	DW	?

MAIN	ENDS
		END